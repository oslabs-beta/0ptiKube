apiVersion: apps/v1
kind: Deployment
metadata:
  name: normal-ops-target
  labels:
    app: normal-ops
    scenario: steady-state
spec:
  replicas: 1
  selector:
    matchLabels:
      app: normal-ops
  template:
    metadata:
      labels:
        app: normal-ops
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "80"
    spec:
      containers:
      - name: normal-app
        image: nginx:alpine
        ports:
        - containerPort: 80
        resources:
          requests:
            memory: "256Mi"    
            cpu: "250m" 
          limits:
            memory: "300"    
            cpu: "300m"        